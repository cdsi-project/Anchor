# === Local AI workspace (WorkBuddy session data) ===
.workbuddy/

# === Generated binaries (derived from .md sources) ===
# PDF exports are generated artifacts; keep sources in git only
*.pdf

# === Runtime / secrets (M2+ installer output) ===
.env
.env.*
*.log
storage/
vendor/
node_modules/

# === Generated passwords (never commit) ===
password/*.pass

# === Deployment-specific values (never commit) ===
# Domain entered at install.sh entry is deployment-specific.
config/domain

# === OS / editor noise ===
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
